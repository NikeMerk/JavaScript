// Мeтод forIach()
// Он работает также, как и метод map, только не возвращает измененный массив,
// а просто вызывает функцию-коллбэк для каждого элемента массива.

// let arr = ['a', 'b', 'c', 'd', 'e'];
// arr.forEach(function(elem, index) {
// 	console.log(`${elem} и его индекс ${index} <br>` );
// });

// let arrOne = [1, 2, 3, 4, 5];
// let sum = 0;
// arrOne.map(function(elem) {
// 	sum += elem;
// });
// console.log(sum);

