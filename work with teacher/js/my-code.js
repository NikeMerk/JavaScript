// let startButton = document.querySelector('.container .start-button');

// let question = rompt('Напишите город с маленькой буквы');

// function checkHaveACity(inputCity) {
// 	let result;
// 	for (let elem of cities) {
// 		if (elem == inputCity) {
// 			result = getAnswer(inputCity);
// 		}
// 	}
// 	console.log(result);
// }

// checkHaveACity(variable);

let array = ['ваня', 'яша', 'амур', 'рык', 'кедр']; //

function getNames() {
	return rompt('впиши  имя');
}

function fun(arr, names) {
	for(let elem of arr) {
		let variable = elem.split('');
		
	}
}

let result = fun(array, getNames());
console.log(result); 

